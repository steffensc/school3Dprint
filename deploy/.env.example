# Copy to deploy/.env and fill in real secrets before `podman compose up`.
POSTGRES_USER=schoolprint
POSTGRES_PASSWORD=change-me
POSTGRES_DB=schoolprint
SCHOOLPRINT_SECRET_KEY=change-me-to-a-long-random-string
SCHOOLPRINT_COOKIE_SECURE=true
